# v2.5 PGO 编译优化总结

## 完成情况

**任务**: PGO 编译优化  
**状态**: ✅ 已完成  
**完成时间**: 2026-06-14

---

## 交付物清单

### 1. CMake 配置 ✅

**文件**: `CMakeLists.txt`

添加了 PGO 支持：
```cmake
# PGO 模式选项
set(PGO_MODE OFF CACHE STRING "PGO mode (OFF/GEN/USE)")

# Profile 数据目录
set(PGO_PROFILE_DIR "${CMAKE_SOURCE_DIR}/pgo_profiles" CACHE STRING "PGO profile directory")
```

支持功能：
- 自动生成 profile 数据
- 使用 profile 数据优化编译
- MSVC 和 GCC/Clang 兼容

---

### 2. PGO 编译脚本 ✅

**文件**: `scripts/build_pgo.bat`

功能：
- `build_pgo.bat all` - 完整 PGO 流程
- `build_pgo.bat gen` - 生成 profile 数据
- `build_pgo.bat use` - 使用 profile 数据编译

---

### 3. 性能对比测试脚本 ✅

**文件**: `scripts/benchmark_pgo.bat`

功能：
- `benchmark_pgo.bat` - 完整性能对比
- `benchmark_pgo.bat baseline` - 基线测试
- `benchmark_pgo.bat pgo` - PGO 测试

输出：
- 基线版本吞吐量
- PGO 版本吞吐量
- 性能提升百分比

---

### 4. PGO 技术文档 ✅

**文件**: `doc/cpp/v2.5-pgo-optimization.md`

内容：
- PGO 技术原理
- 使用方法
- CMake 配置
- 编译器支持
- 性能测试
- 注意事项
- 常见问题

---

### 5. PGO 工作流文档 ✅

**文件**: `doc/cpp/v2.5-pgo-workflow.md`

内容：
- 快速开始
- 详细步骤
- CMake 配置
- 编译命令参考
- 脚本参考
- 性能收益分析
- 文件结构
- 验收标准
- 注意事项
- 常见问题

---

### 6. PGO 总览文档 ✅

**文件**: `doc/cpp/v2.5-pgo-overview.md`

内容：
- 概述
- 文档索引
- 快速开始
- PGO 工作流程
- 编译命令参考
- 脚本参考
- 性能收益分析
- 文件结构
- 验收标准
- 注意事项
- 常见问题

---

### 7. PGO 优化报告模板 ✅

**文件**: `doc/cpp/v2.5-pgo-report.md`

内容：
- 基本信息
- 优化目标
- 测试环境
- 测试数据
- 测试结果
- 优化分析
- 稳定性测试
- 问题与解决
- 验收标准
- 结论

---

## 技术实现

### CMake 配置

```cmake
# [v2.5] PGO (Profile-Guided Optimization) 选项
# PGO_MODE=OFF:  无 PGO（默认）
# PGO_MODE=GEN:  生成 profile 数据（第一次编译）
# PGO_MODE=USE:  使用 profile 数据优化（第二次编译）
set(PGO_MODE OFF CACHE STRING "PGO mode (OFF/GEN/USE)")
set_property(CACHE PGO_MODE PROPERTY STRINGS OFF GEN USE)

# Profile 数据目录
set(PGO_PROFILE_DIR "${CMAKE_SOURCE_DIR}/pgo_profiles" CACHE STRING "PGO profile directory")

# [v2.5] PGO 配置
if(PGO_MODE STREQUAL "GEN")
    # 生成 profile 数据
    message(STATUS "PGO mode: GENERATE profile data")
    target_compile_definitions(orderbook_v2 PRIVATE PGO_GEN=1)

    if(MSVC)
        target_compile_options(orderbook_v2 PRIVATE /GENPROFILE)
        target_link_options(orderbook_v2 PRIVATE /GENPROFILE)
    else()
        target_compile_options(orderbook_v2 PRIVATE -fprofile-generate=${PGO_PROFILE_DIR})
        target_link_options(orderbook_v2 PRIVATE -fprofile-generate=${PGO_PROFILE_DIR})
    endif()

    # 创建 profile 目录
    file(MAKE_DIRECTORY ${PGO_PROFILE_DIR})

elseif(PGO_MODE STREQUAL "USE")
    # 使用 profile 数据优化
    message(STATUS "PGO mode: USE profile data from ${PGO_PROFILE_DIR}")

    if(MSVC)
        target_compile_options(orderbook_v2 PRIVATE /USEPROFILE)
        target_link_options(orderbook_v2 PRIVATE /USEPROFILE)
    else()
        target_compile_options(orderbook_v2 PRIVATE -fprofile-use=${PGO_PROFILE_DIR})
        target_link_options(orderbook_v2 PRIVATE -fprofile-use=${PGO_PROFILE_DIR})
    endif()
else()
    message(STATUS "PGO mode: OFF")
endif()
```

### MSVC 编译选项

```bash
# 生成 profile 数据
cl /GENPROFILE /O2 /GL /arch:AVX2 /utf-8 main.cpp
link /GENPROFILE /LTCG main.obj

# 使用 profile 数据
cl /USEPROFILE /O2 /GL /arch:AVX2 /utf-8 main.cpp
link /USEPROFILE /LTCG main.obj
```

### GCC/Clang 编译选项

```bash
# 生成 profile 数据
g++ -fprofile-generate=./pgo_profiles -O3 -march=native main.cpp

# 使用 profile 数据
g++ -fprofile-use=./pgo_profiles -O3 -march=native main.cpp
```

---

## 使用流程

### 快速开始

```bash
cd cpp v2

# 完整 PGO 流程
scripts\build_pgo.bat all

# 性能对比测试
scripts\benchmark_pgo.bat
```

### 分步执行

```bash
# 第一步：生成 profile 数据
scripts\build_pgo.bat gen

# 第二步：使用 profile 数据编译
scripts\build_pgo.bat use

# 第三步：性能对比测试
scripts\benchmark_pgo.bat
```

---

## 预期收益

### 优化来源

| 优化类型 | 预期收益 | 说明 |
|----------|----------|------|
| 热函数内联 | +3% | 频繁调用的函数自动内联 |
| 分支预测优化 | +2% | 根据实际执行路径优化分支 |
| 循环展开优化 | +2% | 热循环自动展开 |
| 代码布局优化 | +2% | 热代码集中，减少 cache miss |
| 其他优化 | +1% | 指令调度、寄存器分配等 |
| **总计** | **+10%** | |

### 吞吐量目标

| 版本 | 吞吐量 | 提升 |
|------|--------|------|
| v2.4 基线 | 1,047K msg/s | - |
| v2.5 PGO | 1,152K msg/s | +10% |

---

## 文件结构

```
cpp v2/
├── scripts/
│   ├── build_pgo.bat        # PGO 编译脚本
│   └── benchmark_pgo.bat    # 性能对比测试
├── build_pgo/               # PGO 构建目录
│   └── Release/
│       └── orderbook_v2.exe # PGO 优化版本
├── pgo_profiles/            # Profile 数据目录
│   ├── *.gcda               # GCC profile 数据
│   └── *.pgc                # MSVC profile 数据
└── doc/
    └── cpp/
        ├── v2.5-pgo-optimization.md  # PGO 技术原理
        ├── v2.5-pgo-workflow.md      # PGO 工作流
        ├── v2.5-pgo-overview.md      # PGO 总览
        ├── v2.5-pgo-report.md        # PGO 优化报告模板
        └── v2.5-pgo-summary.md       # 本文档
```

---

## 验收标准

- [x] PGO 编译流程完整
- [x] Profile 数据正确生成
- [x] PGO 版本编译成功
- [ ] 性能提升 ≥ 8% (待测试)
- [ ] 10 次重放稳定性测试通过 (待测试)
- [x] 文档完整

---

## 后续工作

### 柯1 (主要负责人)
1. 实施其他优化 (SIMD 加速)
2. 性能测试验证 PGO 收益
3. 整合所有优化

### 柯3 (协助优化)
1. Huge Pages 配置
2. 性能热点分析
3. 优化建议

### 柯2 (基础设施支持)
1. ✅ PGO 编译配置完成
2. ✅ 编译脚本完成
3. ✅ 性能测试脚本完成
4. ✅ 文档完成
5. 待命协助性能测试

---

## 注意事项

### 1. Profile 数据质量

- 使用真实数据收集 profile
- 覆盖所有代码路径
- 多次运行取平均值

### 2. 编译时间

- PGO 编译需要两次编译
- 第一次编译时间增加 10-20%
- 第二次编译时间增加 5-10%

### 3. 文件大小

- Profile 数据文件较大（10-100 MB）
- 需要足够的磁盘空间

### 4. 调试

- PGO 版本调试信息可能不完整
- 建议保留普通版本用于调试

---

## 参考资料

1. [MSVC PGO 文档](https://learn.microsoft.com/en-us/cpp/build/profile-guided-optimizations)
2. [GCC PGO 文档](https://gcc.gnu.org/onlinedocs/gcc/Optimize-Options.html)
3. [LLVM PGO 文档](https://llvm.org/docs/HowToBuildWithPGO.html)
4. [PGO 最佳实践](https://clang.llvm.org/docs/UsersManual.html#profile-guided-optimization)

---

**文档版本**: v1.0  
**最后更新**: 2026-06-14  
**作者**: 柯2 (编译工具链与系统优化专家)